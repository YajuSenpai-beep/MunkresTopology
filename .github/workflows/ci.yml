name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          cd latex-index-tool
          pip install -e ".[dev]"
      - name: Run tests
        run: cd latex-index-tool && python -m pytest tests/ -v
      - name: Lint
        run: cd latex-index-tool && ruff check latex_index/
      - name: Type check
        run: cd latex-index-tool && mypy latex_index/ --strict

  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          cd latex-index-tool
          pip install -e ".[dev]"
      - name: Run benchmarks
        run: |
          cd latex-index-tool
          python -m pytest tests/test_performance.py -v --tb=short -W default \
            --timeout 120 \
            -k "not slow" || echo "WARNING: benchmark regressions detected"
          echo "Performance benchmark completed."
          echo "Hard thresholds: 500p 20k entries < 30s, insertion < 5s, generation < 60s"
      - name: Verify thresholds
        run: |
          cd latex-index-tool
          python -c "
          # Verify basic performance sanity
          import time, sys
          from latex_index.engine import IndexEngine
          config = {'templates': {'l1': r'\\index{\${key}}'}, 'aliases': {}, 'math_shortcuts': {}}
          engine = IndexEngine(config)
          content = 'word ' * 25000  # ~125KB text
          entries = [{'term': f'w{i:04d}', 'level': 1} for i in range(1000)]
          t0 = time.perf_counter()
          ops = engine.find_insertions(content, entries)
          elapsed = time.perf_counter() - t0
          threshold = 5.0
          if elapsed > threshold:
              print(f'FAIL: insertion took {elapsed:.1f}s > {threshold}s limit')
              sys.exit(1)
          print(f'OK: insertion {elapsed:.1f}s < {threshold}s')
          "

  mutation:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          cd latex-index-tool
          pip install -e ".[dev]"
          pip install mutmut
      - name: Run mutation tests (sampled)
        run: |
          cd latex-index-tool
          mutmut run --paths-to-mutate latex_index/matcher.py --runner "python -m pytest tests/test_matcher.py -q" || true
          mutmut run --paths-to-mutate latex_index/parser.py --runner "python -m pytest tests/test_parser.py -q" || true
          mutmut run --paths-to-mutate latex_index/scanner.py --runner "python -m pytest tests/test_scanner.py -q" || true
          echo "Mutation testing completed. Review results above."
          echo "Target: >80% mutation score on core modules."

  docker:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t latex-index-tool latex-index-tool/
      - name: Push to GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker tag latex-index-tool ghcr.io/${{ github.repository }}/latex-index-tool:${{ github.ref_name }}
          docker push ghcr.io/${{ github.repository }}/latex-index-tool:${{ github.ref_name }}

  pypi:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Build package
        run: |
          cd latex-index-tool
          pip install build
          python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: latex-index-tool/dist/
          password: ${{ secrets.PYPI_API_TOKEN }}
