% 图 13.2 —— 手工重建（两个相交矩形 B′₁ B′₂，交点处一个元素点 x）
%%
%% ══ 按用户意见重做（2026-09-19）══
%%  ①「元素 B2 缺少一撇」：原书两个标签**都是带撇的** B′₂、B′₁。
%%     B′₁ 的撇被 prims2 拆成了一条独立的短线段（(52,210)-(54,204)）所以还在；
%%     B′₂ 的撇则被并进了字形框（实测框宽 26.7，而纯 B 只有 20.0）——
%%     probe 于是拿它当「一个特别宽的 B」处理，字号给到 36.1，撇就丢了。
%%     现在两个撇都**直接写进节点文字**（`B′`）—— 文字就该当文字写，
%%     既不是残段、也不用另量尺寸画线（2026-09-25 曾走错这一步，已纠正）。
%%  ②「下标的重叠」：同上，B₂ 的字号被撑大后 ₂ 挤进了 B 的右下角。
%%     现在 B 与 B₁ 同字号（30.0），下标按原图字形框单独定位。
%%  ③「B1 的撇画记号需要核实是否合适」：实量原图，B′₁ 的撇是**独立连通域**
%%     （框 5.0x8.3，中心 (48.70,201.77)，即 B 的右上角外侧），方向是下左->上右，
%%     宽约 2.5 画布单位 —— 确认是撇，按这个几何重画。
%%
%% ⚠ 与 13.1 同一个毛病：probe 的图形参数比原书**小约 2~2.5%**
%%   （矩形 A 实量 288.7x177.8，tex 画出来只有 282.8x174）。本图一律自量。
%%   标定 SC = 733/408 = 1.7966 px/画布单位。
%%
%% 线宽：矩形实量 5.0；撇实量约 2.6。
\documentclass[12pt]{standalone}
\usepackage{fontspec}
\setsansfont{Arial}
\newfontfamily\mfallback{DejaVu Sans}
\usepackage{tikz}
\begin{document}
\begin{tikzpicture}[x=1pt, y=-1pt, line width=5.0pt, line cap=round, line join=round]

  %% ════ 两个矩形（边线中心线实测值）════
  \draw ( 9.7, 10.2) rectangle (298.4,188.0);
  \draw (174.4,102.1) rectangle (349.8,394.4);

  %% ════ 元素点 ════
  \fill (212.4,127.2) circle (6.4);

  %% 撇（U+2032）已改回**随字母一起打**（见下方 B′ 节点）：
  %%   2026-09-25 曾一度把它当成独立图元「量尺寸另画一根线」—— 那是把文字当图形了。

  %% ════ 字母 / 数字 ════
  \node[anchor=center,inner sep=0pt,fill=white,font=\fontsize{30.1}{37.6}\selectfont] at (211.8,152.0) {\textsf{\textbf{\textit{x}}}};
  \node[anchor=center,inner sep=0pt,fill=white,font=\fontsize{30.2}{37.8}\selectfont] at (34.5,207.9) {\textsf{\textbf{\textit{B}′}}};
  \node[anchor=center,inner sep=0pt,fill=white,font=\fontsize{21.6}{26.9}\selectfont] at (52.3,223.8) {\textsf{\textbf{1}}};
  \node[anchor=center,inner sep=0pt,fill=white,font=\fontsize{30.0}{37.6}\selectfont] at (376.3,217.9) {\textsf{\textbf{\textit{B}′}}};
  \node[anchor=center,inner sep=0pt,fill=white,font=\fontsize{23.6}{29.6}\selectfont] at (395.2,232.1) {\textsf{\textbf{2}}};

  % 钉死画布：否则超出的图元/控制点会把页面撑大，1:1 回比就失准
  \pgfresetboundingbox
  \path[use as bounding box] (0,0) rectangle (408,408);
\end{tikzpicture}
\end{document}
