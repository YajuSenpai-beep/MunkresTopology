你是 LaTeX 工具链专家和高性能 Python 开发者。我们有一个非常成熟的 LaTeX 索引自动插入工具（latex-index-tool），已经具备：
- 完整的 Python 包（12 个模块）
- YAML 配置驱动，支持别名、数学快捷方式、多种模板
- 324 项 pytest 测试，87% 覆盖率，含模糊测试
- 性能自适应（小规模逐条搜索，大规模 Aho-Corasick 自动机）
- 原子写入、文件锁、批处理容错、编码自动检测、换行符保留
- CLI 子命令（insert, parse, scan, tools）
- CI/CD 发布 PyPI、Docker、PyInstaller 打包
- README 完善的文档

请在此基础上，给出**下一阶段的进化计划**，重点是：
1. **LaTeX 语义覆盖**：检查是否还有未覆盖的环境（如 `\ExplSyntaxOn/Off`、`tikzpicture`、`pgfplots` 等），给出应跳过的所有区域补全方案。
2. **索引高级格式**：完善 `\index{key|see{other}}`、`\index{key|seealso{other}}`、范围索引 `|(` `|)` 的自动配对检测与验证。
3. **与 LaTeX 编译集成**：生成 `latexmkrc` 或 `arara` 规则，让用户可以在编译前自动插入索引，并提供回滚选项。
4. **交互体验升级**：如果用户使用 `--interactive`，提供一个基于 `rich` 的 TUI 界面，可多选/全选/搜索词条。
5. **报告与诊断**：增加 `latex-index report` 命令，生成索引覆盖率、缺失词条、重复索引的分析报告。
6. **稳定性与测试**：将测试覆盖率从 87% 提升到 95% 以上，补充针对 LaTeX3 语法的测试用例。添加变异测试（mutmut）评估测试质量。
7. **国际化排序增强**：支持配置自定义排序规则文件（如 xindy 的 .xdy 生成），并处理多语言混合索引。

要求：
- 先输出**现状评估**，指出当前项目在这些新维度上的不足和风险。
- 给出**优先级排序的改进清单**（P0 必须，P1 重要，P2 可选）。
- 对 P0 和 P1 项目，提供**具体实现方案**和预期的代码模块变更。
- 确保所有建议不破坏现有 324 项测试，并给出需要新增的测试用例类型。
- 最后，输出修改后的 `README.md` 片段（新增功能的文档部分）。

请开始分析。