你现在是 LaTeX 排版与 Python 工程专家。我需要你帮我将现有的“LaTeX 正文内容索引自动插入脚本”重构成一个专业、稳定、可靠的工具，并建立完整的测试体系。

## 现有脚本概况
- 功能：读取 .tex 文件，识别指定词条，在正文适当位置插入 \index{词条} 命令。
- 问题：目前只是“能跑”的原型，缺乏错误处理、日志、测试和工程结构，健壮性不足。
- 目标：让它成为可用于生产环境（大型书籍项目）的命令行工具。

## 需要你完成的任务（请完整交付以下内容）

### 1. 代码审查与重构规划
- 先假设我发给你现有代码，请你指出所有潜在 bug、不稳定点、不符合 LaTeX 语义的处理方式。
- 提出一份模块化重构方案，将代码拆分为合理模块（如词条识别器、插入引擎、配置管理、文件读写工具等）。
- 所有模块使用 Python 3.10+ 类型提示，并编写清晰的 docstring。

### 2. 健壮性与防御性编程
- 任何异常情况（文件不存在、编码错误、LaTeX 语法严重破损）都必须捕获，并通过日志输出清晰的中文错误信息，**绝不允许脚本直接崩溃**。
- 文件写入必须采用“先写临时文件，成功后再替换原文件”的原子写入方式，避免损坏用户原稿。
- 提供 `--dry-run` 模式：只分析文件并列出即将插入的索引条目和位置，不实际修改文件。
- 处理路径中的空格、中文、特殊字符。
- 处理超大文件时使用流式处理或缓冲读写，避免内存溢出。

### 3. 配置与可维护性
- 将词条列表、排除规则（如忽略词）、索引格式（如是否使用子条目）、文件匹配模式等所有配置抽取到一个独立的 `config.yaml` 文件中。
- 脚本通过命令行参数指定配置文件路径、输入文件/目录和输出选项。
- 保留默认配置，用户不提供时也能正常运行。

### 4. 日志系统
- 实现三级日志：INFO（正在处理哪个文件、插入了几条索引）、WARNING（跳过某个词的原因，如已在数学模式中）、ERROR（无法处理的文件，跳过并记录）。
- 日志同时输出到控制台和文件（`index_tool.log`），并可配置日志级别。

### 5. LaTeX 语义正确性（最高优先级）
- 只在“正文文本”中插入索引，**绝对不**在以下位置插入或提取词条：
  - 数学模式：`$...$`、`$$...$$`、`\(...\)`、`\[...\]`、`\begin{equation}...\end{equation}` 等。
  - 注释：`%` 后的同行内容。
  - 抄录环境：`\begin{verbatim}...\end{verbatim}`、`\begin{lstlisting}...\end{lstlisting}` 等。
  - 已有的 `\index{...}` 命令内部（防止重复插入）。
  - 各类命令的必选/可选参数内（如 `\section{...}` 的标题是否要索引可配置，默认开启但需确保生成合法的 `\index` 位置）。
- 能正确处理嵌套花括号、转义字符、`\newcommand` 等自定义命令。
- 生成的 `\index` 条目必须对特殊字符（`!`、`@`、`|`、`"` 等）进行 LaTeX 转义。

### 6. 完整的测试体系（使用 pytest）
请同时交付以下测试文件和说明：
- **单元测试**：对词条识别、插入逻辑、特殊字符转义、环境判断等核心函数编写独立测试，每种情况至少一个用例。
- **集成测试**：提供至少 5 个具有代表性的 `.tex` 样本文件（覆盖常规文档、含数学/注释/抄录、多文件项目、含自定义命令等），并对每个样本给出期望的输出文件，用 pytest 自动化比对。
- **回归测试**：收集你在修复 bug 过程中发现的所有边界用例，每个用例对应一个最小 `.tex` 样本，确保不会再次出错。
- **异常测试**：输入损坏的 `.tex`（花括号不匹配）、二进制文件、空文件、超大术语列表等，验证脚本不崩溃并给出合理报错。
- **性能测试**：提供脚本生成一个约 500 页的随机文本 .tex 文件的方法，并测试插入上万个词条时的运行时间和内存占用，给出性能基准报告。

### 7. 文档与使用方式
- 输出一份清晰的中文 `README.md`，包含安装依赖、配置说明、命令行参数列表、使用示例、日志说明和如何运行测试。
- 提供一个 `Makefile` 或 `pyproject.toml`，可通过一条命令执行所有测试和代码风格检查（如 ruff 或 flake8）。

## 输出格式
请分多个代码块给出最终交付物：
1. 重构后的完整 Python 脚本模块（请说明每个文件的作用）
2. `config.yaml` 模板
3. 所有测试文件（`test_*.py`）和示例 `.tex` 样本内容
4. `README.md` 全文
5. 性能测试脚本和基准报告模板
6. 总结本次重构解决了哪些原型的缺陷，以及生产环境下还需注意的事项。

如果你在一次回答中无法输出全部内容，可以分步进行：先给重构方案和模块结构，我确认后再输出实现代码。但请至少在一次回答中给出重构方案和测试策略的完整文本，以便我先了解整体设计。