你现在是 LaTeX 排版与 Python 工程专家，对构建自动化工具和生产级软件有丰富经验。我们有一个 LaTeX 索引自动插入工具集（`index/`），已具备完整的 Python 包、配置驱动、三级日志、原子写入和 82 项 pytest 测试。现在需要你对其进行**进阶优化和包装**，使其达到可分发、可维护、可扩展的专业工具标准。

## 当前工具概况
- 核心 Python 包：`latex_index/`（engine, parser, tex_utils, config, cli）
- 测试体系：82 项（单元/集成/回归/异常/性能），全部通过
- 配置文件：YAML 驱动，支持别名、数学快捷方式等
- 安全特性：dry-run、原子写入、三级日志
- 遗留 Node.js 引擎（`core/`）和散落工具脚本（`tools/`）

## 需要你完成的改进任务（请交付具体实施方案和代码）

### 1. 代码质量与工程化
- 为 Python 包增加完整的类型提示检查（mypy strict 模式），并修复所有类型问题。
- 使用 `ruff` 或 `flake8` 统一代码风格，生成相应配置文件（`pyproject.toml` 中集成）。
- 将现有脚本中的魔法字符串、硬编码路径全部抽取为常量或配置项。
- 重构 `tools/` 中的批处理脚本，将它们模块化并归入 Python 包或提供统一 CLI 入口。

### 2. 功能增强
- 实现真正的**多文件项目支持**：可接收目录或主文件（如 `main.tex`），自动解析 `\input`/`\include`，按顺序处理所有子文件，保留交叉引用顺序。
- 增加**交互式确认模式**：`--interactive`，在 dry-run 后逐条询问用户是否插入，支持 y/n/skip all。
- 增加**索引词自动发现**功能：扫描现有 `.tex` 文件中已出现的 `\index{...}`，汇总成词条列表，供用户审查后作为输入。
- 支持 `\index` 的 `|see{...}` 和 `|seealso{...}` 格式，并在配置中可定制。
- 智能避开 `\label`、`\ref`、`\cite` 等命令的参数区域（当前已避开大部分，需补全所有标准 LaTeX 命令）。

### 3. 性能优化
- 对 500 页以上文档的性能测试表明，当前处理时间随词条数线性增长。请你分析 `engine.find_insertions` 的复杂度，并给出优化方案（如使用 Aho-Corasick 自动机进行多模式匹配）。
- 实现文件流式处理，避免一次性加载整个文件内容（当前对大文件已采用缓冲，但需进一步优化内存使用）。
- 增加进度条显示（`tqdm`）以提升用户友好性。

### 4. 鲁棒性与边界情况
- 处理各种 LaTeX 编码：UTF-8、Latin-1 等，自动检测编码并正确读写。
- 处理 Windows 与 Unix 换行符（`\r\n` vs `\n`），插入索引后保持原文件换行风格不变。
- 增加对 `\includeonly`、`\include` 嵌套、`\input` 带空格/扩展名的健壮解析。
- 在日志中添加文件名和行号，便于定位问题。

### 5. 文档与示例
- 更新 `README.md`，使其包含：
  - 动图演示（建议用 asciinema 录制）
  - 完整配置手册（每个参数的解释和示例）
  - 常见工作流：从扫描 OCR 索引文本到插入的端到端教程
  - 贡献指南（如何运行测试、添加新功能）
- 生成 `CHANGELOG.md`，用自动化方式基于 Git 提交历史生成。
- 编写一个 `Makefile`，包含 `make test`、`make lint`、`make build`、`make clean`。

### 6. 打包与分发
- 完善 `pyproject.toml`，使工具可通过 `pip install` 安装，并提供命令行入口 `latex-index`。
- 提供 PyInstaller 配置（`.spec` 文件），可生成独立的可执行文件，方便无 Python 环境的用户。
- 编写 `Dockerfile`，构建最小化 Docker 镜像，挂载项目目录后直接运行。
- 配置 GitHub Actions，实现：
  - 推送时自动运行测试和 lint
  - 发布 tag 时自动构建 Docker 镜像并推送到 GitHub Container Registry
  - 可选：自动发布到 PyPI

### 7. 国际化（可选但加分）
- 支持生成中文索引（需处理拼音排序或笔画排序），配置中可指定 `index_processor`（makeindex/xindy）。
- 使 CLI 日志信息支持中英文切换（通过配置文件或环境变量）。

## 输出要求
- 将上述任务分解为可逐步实施的具体步骤，排定优先级（P0/P1/P2）。
- 对于每个改进项，给出关键代码片段或伪代码，以及需要的配置文件变更。
- 确保所有改进后，现有的 82 项测试仍然通过，并补充新的测试用例覆盖新增功能。
- 最终交付一个完整的项目更新包，包括文件结构、代码变更、文档增补和 CI 配置。

请开始分析当前项目状态，并给出你的详细优化计划（先计划，待我确认后再输出完整代码）。如果一次无法输出全部，请先提供核心改进：性能优化和多文件支持。